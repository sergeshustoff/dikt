buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
        jcenter()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath 'com.github.jengelman.gradle.plugins:shadow:5.0.0'
        classpath 'com.vanniktech:gradle-maven-publish-plugin:0.17.0'
        classpath "io.github.sergeshustoff.dikt:dikt-gradle-plugin:$dikt_sample_version"
    }
}
allprojects {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}
